# --- CMake build artifacts (recursive — protects against accidental in-source builds) ---
build/
CMakeFiles/
CMakeCache.txt
CMakeConfigureLog.yaml
cmake_install.cmake
Makefile
compile_commands.json
_deps/

# --- Editor / env ---
.direnv/
.cache/
.claude/settings.local.json

# --- Python (if anything pulls in pybind etc.) ---
*.egg-info/
__pycache__/
.python-version

# --- Dist / shared objects ---
dist/
*.so
